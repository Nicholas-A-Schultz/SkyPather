# SkyPather
SkyPather is an AI-driven flight route planning system designed to optimize navigation for piston aircraft operating under instrument flight conditions (IFR). The project models aircraft routing as a graph-based search problem, where waypoints serve as nodes and flight paths as edges.
